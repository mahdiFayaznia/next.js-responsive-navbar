import "./globals.css";
import type { Metadata } from "next";
import { Inter } from "next/font/google";
import Header from "./components/Header";
import HeaderMobile from "./components/HeaderMobile";
import SideNav from "./components/SideNav";
import PageWrapper from "./components/PageWrapper";
import MarginWidthWrapper from "./components/MarginWidthWrapper";

const inter = Inter({ subsets: ["latin"] });

const rtlDirection = false; // default: ltr  -->  set true for rtl: true & set false for ltr

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`bg-white ${inter.className}`}>
        <div className="flex">
          <SideNav rtl={rtlDirection} />
          <main className="flex-1">
            <MarginWidthWrapper rtl={rtlDirection}>
              <Header rtl={rtlDirection} />
              <HeaderMobile rtl={rtlDirection} />
              <PageWrapper rtl={rtlDirection}>{children}</PageWrapper>
            </MarginWidthWrapper>
          </main>
        </div>
      </body>
    </html>
  );
}
